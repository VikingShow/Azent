# Azent 设计评估与审视

## 一、四象限模型：核心哲学与合理性分析

### 1.1 模型的真正意图

四象限模型不是简单的分类工具。它的核心前提是一个**被主动接纳的事实**：

> Agent 的上下文是有限的，注意力是有限的，记忆是会衰减的。这不是需要"解决"的 bug，而是需要**工程设计**来应对的永久约束条件。

这个前提在认知科学中有坚实的根基：
- **Cowan 2001**: 人类工作记忆约 4 个独立块，不是 7 个
- **Peterson & Peterson 1959**: 未复述信息约 20 秒内衰减
- **Simons & Chabris 1999**: 注意力是窄束——"看不见的大猩猩"
- **Liu et al. 2023**: LLM 的上下文窗口存在 "Lost in the Middle" 效应

人类和 LLM 在这一点上是同构的：**工具很小，很窄，会漏。** 任何只能靠"记住"来执行的规则，最终都会被遗忘。

### 1.2 四象限作为注意力维持状态机

```
                Agent 遵循指令          Agent 未遵循
              ┌────────────────────┬────────────────────┐
显式声明的    │  Q1: 理想状态        │  Q3: 上下文漂移     │
              │  声明 + 遵循         │  声明了但被遗忘     │
              ├────────────────────┼────────────────────┤
未显式声明的  │  Q2: 隐式知识        │  Q4: 默认安全       │
              │  训练数据/惯例驱动    │  无需干预           │
              └────────────────────┴────────────────────┘
```

这个模型的真正价值不在于分类，而在于 **状态转移**：

```
Q4 (默认安全)
  │ Agent 基于隐式知识采取行动
  ▼
Q2 (隐式知识驱动 — 危险状态)
  │ Gate 拦截 → 要求声明边界
  │ Agent 必须回答: 我理解了什么？我假设了什么？
  │                 我的知识从哪里来？我有多确定？
  │                 我不知道什么？
  ▼
Q1 (声明 + 遵循 — 理想状态)
  │ 上下文稀释 → 指令被遗忘
  ▼
Q3 (上下文漂移 — 已声明但未遵循)
  │ checkDrift 检测 → escalate 关 Gate
  │ Pins 重新注入 → 强制重新声明
  ▼
Q1 (回到理想状态)
```

**每一对状态转移都是一次注意力维持操作**：
- Q2→Q1: **外化**——将隐式知识变为显式声明
- Q1→Q3→Q1: **检测 + 纠正**——发现注意力漂移后重新锚定

### 1.3 "不确定时必须向人类确认"原则

这是用户设计哲学中最关键的一条。它的含义是：

> Agent 不应该在低置信度下自主行动。任何不确定性都必须升级为人类确认。

这对应到三个具体机制：

| 机制 | 触发条件 | 当前实现 | 缺口 |
|------|---------|---------|------|
| **Gate: clarify** | 边界已声明但置信度为 "low" | 返回 clarify 结果 + 建议问题 | ❌ 未强制执行——Agent 可以忽略 clarify 继续执行 |
| **Gate: escalate** | 检测到 critical 指令被违反 | 自动关 Gate，强制重新声明 | ✅ 已实现，但 escalate 后 Agent 可能仍然不向人类确认 |
| **zen_boundary unknowns** | Agent 声明了不知道的内容 | 返回建议问题列表 | ❌ 问题未被自动呈现给用户——Agent 需要主动调用 question 工具 |

**核心缺口**：系统的结构已经就位（Gate 有 clarify/escalate 类型，boundary 有 unknowns 字段），但**人类确认环节是断裂的**。Gate 返回 clarify 后，没有机制**自动**触发人机交互。Agent 可以"看"到 clarify 结果但选择忽略。

### 1.4 模型的合理性评价

**合理之处**：

1. **接纳约束而非对抗约束**。大多数 AI agent 框架试图通过更大的上下文窗口、更长的系统提示词来"克服"注意力限制。四象限模型走相反的路：接纳注意力是有限的，然后通过结构来弥补。这是工程思维的正确方向。

2. **Q2 的洞察是深刻的**。LLM 天然带有大量隐式知识（训练数据中的模式、项目惯例、上下文推断）。这些知识在未被显式化之前是不可测试、不可审计的。`zen_boundary` 将隐式知识**表面化**——这本身就是一种安全机制。

3. **Q3 的洞察同样深刻**。"Lost in the Middle" 是一个被充分记录的 LLM 失败模式，但大多数工具只是希望在提示词中强调"请注意这一点"——这恰恰是问题所在（强调本身也会被遗忘）。指令钉扎（pinning）是**结构性的**解决方案：在每一个安全边界重新注入。

4. **状态机提供了一个清晰的思维模型**。开发者可以理解 Agent 在任何时刻处于哪个象限，以及需要什么操作来转移到更安全的状态。

**可以质疑的地方**：

1. **Q1 是理想化的**。"Agent 完全遵循指令" 是一个极限状态，在实践中很难达到。即使声明了边界、钉扎了指令，Agent 仍然可能产生不符合预期的输出。Q1 更像是一个**方向**而非一个可达状态。

2. **Q4 的边界模糊**。"不需要干预" 和 "应该干预但还没发现" 在实践中很难区分。一个 Agent 可能因为上下文漂移（Q3）而忽略了指令，但系统可能没有检测到。

3. **模型的粒度可能不够**。当前的四象限是 Agent 级别的。但实际场景中，同一个 Agent 在不同的知识域可能有不同的置信度。一个更细粒度的模型（per-domain quadrant）可能更有用。

**总体评价**：四象限模型**有明确的学术基础和工程价值**。它不是完美的，但比"写好提示词然后希望 Agent 记住"前进了一大步。核心贡献是**将注意力维持从"人的责任"变成了"系统的责任"**。

---

## 二、当前实现的完整审视

### 2.1 端到端可用的路径

```
用户启动会话
  │
  ├─ zen.init() → 创建 ZenState (gate=closed, pins=[], confidence=unknown)
  ├─ 自动钉扎 → 扫描 AGENTS.md 中的 CRITICAL:/NEVER:/MUST: 约束 → zen.pin()
  ├─ 恢复持久化状态 → 从 .azent/data/zen/{sessionID}.json 加载
  │
  ▼
用户输入 → runLoop()
  │
  ├─ reminders.ts → renderContext() 注入 <zen_layer> 到用户消息
  │   └─ Gate 状态、Boundary、Pins、Capabilities 每轮可见
  │
  ├─ LLM 调用
  │
  ├─ 工具结算:
  │   └─ 破坏性工具 (edit/write/bash/shell/apply_patch):
  │       └─ zen.gate() → block → 返回错误 (✅ 已强制执行)
  │       └─ zen.gate() → clarify → ? (❌ 未强制执行)
  │       └─ zen.gate() → escalate → ? (❌ 未强制执行)
  │       └─ zen.gate() → warn → ? (❌ 未强制执行)
  │
  ├─ 漂移检测:
  │   └─ checkDrift() → critical 违规 → escalate 关 Gate → drift alert
  │
  └─ 持久化 → exportState() → 写入 JSON 文件
```

### 2.2 每条路径的评估

| 路径 | 状态 | 评估 |
|------|------|------|
| Boundary 声明 (Q2→Q1) | ✅ 可用 | `zen_boundary` 工具已注册，Gate 强制拦截未声明边界的破坏性操作。**但** `clarify` 和 `warn` 结果未被强制执行。 |
| 指令钉扎 (Q3 防护) | ✅ 可用 | `/pin` 命令 + `zen_pin` 工具 + 自动钉扎。钉扎的指令每轮通过 `renderContext()` 重新注入。 |
| 漂移检测 + 升级 | ✅ 可用 | `checkDrift` 检测违规，critical 违规触发 `escalate` 关 Gate。**但** LLM 深度检测 (`checkDriftDeep`) 无调用者。 |
| 人类确认链路 | ⚠️ 断裂 | 系统有能力检测到需要人类确认的场景（clarify, unknowns, escalate），但**没有自动触发人机交互的机制**。 |
| V2 路径 | ❌ 未集成 | V2 session runner 有 TODO 标记但无 Zen 集成。当前仅 V1 路径有效。 |
| 持久化 | ✅ 可用 | 文件 JSON 存储，会话重启后恢复。 |
| Experience 记忆 | ✅ 可用 | TF-IDF 搜索，`<zen_experience>` 注入到 plan mode。 |

### 2.3 最关键的缺口：人类确认链路断裂

这是用户设计哲学中最核心的部分——"不确定时必须向人类确认"——但在当前实现中**没有闭环**。

具体来说：

```
当前行为:
  Gate 返回 clarify → Agent 收到 clarify 结果
    → Agent 可以调用 question 工具询问用户 ← 但这是 Agent 的"选择"，不是"强制"
    → Agent 也可以忽略 clarify，尝试其他方式绕过 Gate

应有的行为:
  Gate 返回 clarify → 系统自动触发人机交互
    → 用户看到: "Agent 对 [topic] 不确定，建议确认: [question]"
    → 用户回答 → Gate 根据用户确认决定 allow/block
    → Agent 不能绕过这一步
```

---

## 三、Azent 是否好用？可用性评估

### 3.1 从开发者（用户）视角

**好的方面**：
- `/pin NEVER use console.log` 很直观——用户可以随时钉扎约束
- `/zen` 可以查看当前 Gate 状态——透明度好
- 破坏性操作被 Gate 拦截时有明确提示——不会静默失败

**不好的方面**：
- 用户体验不到 Zen Layer 的"保护感"——被 Gate 拦截时，Agent 只是返回一段文本说被 blocked，用户可能不知道发生了什么
- 没有视觉指示器显示当前 Gate 状态（TUI 中）
- `/pin` 钉扎后，用户无法直观看到"这条指令正在保护我"
- Agent 如果反复被 Gate 拦截（比如不知道怎么调用 `zen_boundary`），用户体验会很差

### 3.2 从 Agent 视角

**好的方面**：
- `<zen_layer>` 每轮注入上下文，Agent 可以"看到"自己的状态
- `zen_boundary` 的结构清晰——理解、假设、知识来源、计划、未知项
- `zen_pin` 让 Agent 可以主动保护关键指令

**不好的方面**：
- Gate 的 block/clarify/warn 对 Agent 来说是一段文本，Agent 可能不理解如何正确响应
- 如果 Agent 被 block 后不知道怎么声明边界，会陷入死循环
- `zen_boundary` 要求填写的字段很多（understanding, assumptions, implicitKnowledge, plan, unknowns），可能被认为是"繁文缛节"

### 3.3 与现有工具的对比

| | Azent | Claude Code | Cursor | GitHub Copilot |
|---|-------|-------------|--------|---------------|
| 破坏性操作保护 | ✅ Gate 强制拦截 | ❌ 依赖提示词约束 | ⚠️ 有 diff 预览但无强制 | ❌ 无 |
| 指令漂移防护 | ✅ pin + checkDrift | ❌ 无 | ❌ 无 | ❌ 无 |
| 隐式知识表面化 | ✅ zen_boundary | ❌ 无 | ❌ 无 | ❌ 无 |
| 人机确认 | ⚠️ 有机制但未强制执行 | ✅ askUserQuestion | ✅ 内联确认 | ❌ 无 |
| 多阶段编排 | ✅ Loop Mode | ❌ 无 | ❌ 无 | ❌ 无 |
| 经验记忆 | ✅ Experience Store | ❌ 无 | ❌ 无 | ❌ 无 |

Azent 在**理论上**提供了最全面的安全保护。但在**人机确认**这个关键环节上，Azent 落后于 Claude Code（后者有完整的人机确认机制）。这是一个需要优先修复的缺口——因为四象限模型的核心前提恰恰是"不确定时必须向人类确认"。

---

## 四、是否有更好的方式？

### 4.1 当前方式 vs. 可能的替代方案

**当前方式：Agent 主动声明**
- Agent 调用 `zen_boundary` 声明边界 → Gate 检查 → 允许/拦截
- 优点：Agent 是"自觉"的，声明本身就有认知价值
- 缺点：依赖 Agent 的理解和配合；Agent 可能不知道如何声明或选择不声明

**替代方案 A：系统强制注入**
- 系统在每次破坏性操作前自动注入 "请确认以下内容..."
- 优点：不依赖 Agent 主动性
- 缺点：增加延迟，且可能被 Agent 忽略（这恰恰是 Q3 的问题）

**替代方案 B：用户预授权**
- 用户在会话开始时声明 "允许编辑 *.ts 文件" / "禁止删除文件"
- 优点：明确、可审计
- 缺点：粒度粗，用户可能无法预见所有场景

**替代方案 C：混合模式（推荐）**
- 用户预授权提供**粗粒度安全边界**（允许编辑代码，禁止执行部署脚本）
- Agent 边界声明提供**细粒度上下文**（我理解我要修改 X，我假设 Y，我不确定 Z）
- Gate 综合两者做决策
- 当 Agent 不确定（confidence=low 或 unknowns>0）→ **强制**人类确认

### 4.2 结论

当前方式（Agent 主动声明 + Gate 系统强制）的**方向是正确的**，但缺失了**强制人类确认**这一关键环节。混合模式（预授权 + 声明 + 强制确认）是最优方案。

---

## 五、优化建议（按优先级排序）

### 🔴 P0 — 修复人类确认链路

**问题**: Gate 返回 clarify/escalate/warn 后，没有自动触发人机交互。

**建议**:
1. 在 `tools.ts` 中，Gate 返回 `clarify` 时，**自动调用 question 工具**将 unknowns 呈现给用户
2. Gate 返回 `escalate` 时，**通知用户** "Agent 违反了钉扎指令 [X]，Gate 已关闭，需要重新声明边界"
3. 用户确认后才能重新打开 Gate

### 🟠 P1 — 改进 Agent 体验

**问题**: Agent 被 block 后可能不知道如何正确响应。

**建议**:
1. 在 `<zen_layer>` 中为 Agent 提供**明确的下一步指令**——不只是 "Gate is CLOSED"，而是 "要打开 Gate，调用 zen_boundary 声明你的理解、假设和未知项"
2. 第一次被 block 时，在返回消息中包含一个**示例 zen_boundary 调用**
3. 如果 Agent 连续 3 次被 block，自动切换到人类确认模式

### 🟡 P2 — 改进用户体验

**问题**: 用户看不到 Zen Layer 的保护效果。

**建议**:
1. TUI 中添加 Gate 状态指示器（🔒/🔓 图标）
2. 钉扎指令在 `/zen` 输出中高亮显示
3. 漂移检测触发时，TUI 中显示警告通知
4. 统计面板：显示 "Gate 拦截了 X 次未声明边界的操作" / "检测到 Y 次指令漂移"

### 🟡 P3 — checkDriftDeep 激活

**问题**: LLM 深度漂移检测有实现但无调用者。

**建议**:
1. 在 V2 session runner 中为深度检查提供 LLM 基础设施
2. 使用两级策略：启发式先行（快速），检测到疑似漂移再用 LLM 确认（精准）

### 🔵 P4 — V2 Runner 完整集成

**问题**: V2 session runner 无 Zen 集成。

**建议**: 按已有 TODO 清单逐步完成。

### 🔵 P5 — Phase 级权限强制执行

**问题**: LoopPhase Schema 有 `toolPermissions` 字段但未被使用。

**建议**: 在 sub-agent 创建时，根据当前 phase 的 toolPermissions 过滤工具列表。

---

## 六、总评

Azent 的四象限模型和 Zen Layer 是**有坚实理论基础和明确工程价值的创新**。它的核心洞察——接纳注意力的有限性，用结构来维持注意力——是正确的。

当前实现完成了 ~70% 的愿景。最关键的缺失是**人类确认链路的闭合**：系统有能力检测到 Agent 的不确定性（clarify, unknowns, escalate），但没有强制触发人机交互。这恰恰是四象限模型最核心的设计原则。

如果有且仅有一个需要修复的地方，那就是：**当 Gate 返回 clarify 或 escalate 时，自动触发人机确认，Agent 不能绕过这一步。**

---

*评估日期: 2026-07-02*
*评估范围: Azent v0.3.4 + Phase 1-4 全部改动*
